import { AppShell } from "@/components/layout/app-shell";

export default function InstellingenPage() {
  return (
    <AppShell title="Instellingen">
      <div className="card">
        <p>Hier komen later printerinstellingen, gebruikers en scanner voorkeuren.</p>
      </div>
    </AppShell>
  );
}
